# {PROJECT} · 硬约束（模板）

> 复制为 `<项目>/.cursor/constraints.md` 并填写 `{占位符}`  
> 通用 DoD：`verification-gate` Skill · `.cursor/constraints.md`  
> 由规范库 `scripts/bootstrap-project.ps1` 从本模板生成

---

## 1. 仓库结构

| 目录 | 技术栈 | 职责 |
|------|--------|------|
| `{BACKEND_DIR}/` | {BACKEND_STACK} | |
| `{FRONTEND_DIR}/` | {FRONTEND_STACK} | |
| `docs/` | Markdown | 需求、设计、规范 |

---

## 2. 版本 / 范围边界

- **权威能力文档**：`{CAPABILITY_DOC}`
- **路线图**：`{ROADMAP_DOC}`
- **禁止提前实现**：{OUT_OF_SCOPE_LIST}

---

## 3. 架构不变量（违反即 bug）

1. {INVARIANT_1}
2. {INVARIANT_2}
3. {INVARIANT_3}

---

## 4. 开发习惯

- 最小 diff；匹配现有命名与分层
- 未要求不 commit/push；不提交密钥、`{LOCAL_DB_PATH}`、`.env`
- 完成前：`{FRONTEND_BUILD_CMD}`、`{BACKEND_BUILD_CMD}`
- 改 API 前先读 `{API_DESIGN_DOC}`
- **新能力**：`docs/requirements/features/<id>.md` 状态为 **已定稿** 后方可编码（见 Skill `requirements-refinement`）

---

## 5. 常见陷阱

| 陷阱 | 正确做法 |
|------|----------|
| {TRAP_1} | {FIX_1} |
| 改 backend 不重启 | 重启 `{BACKEND_PORT}` 并冒烟 |
| 新功能走废弃 API | 使用 `{CURRENT_API_CLIENT}` |

---

## 6. 后端重启义务（若 Spring Boot / 类似）

修改 `{BACKEND_DIR}/**` 后交付前必须：

1. 停旧进程（端口 `{BACKEND_PORT}`）
2. `{BACKEND_RUN_CMD}`
3. 确认 migration 成功
4. HTTP 冒烟改动接口

---

## 7. 完成定义（DoD）

- [ ] 范围符合 capability（`scope-check`）
- [ ] `verification-gate` 已执行
- [ ] backend build/test；若改 backend 则重启+冒烟
- [ ] frontend build
- [ ] 契约已同步 `docs/design/`
- [ ] 未提交密钥与本地库文件
